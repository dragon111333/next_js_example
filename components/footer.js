const Footer = (prop)=>{
    const userName = (prop.userName==null)?"no login":prop.userName;
    return(
            <footer className="bg-tomato bg-100">
                <center>
                    <h1>hello</h1>
                </center>
            </footer>
    );
}
export default Footer;